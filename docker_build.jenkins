pipeline {
    agent none
    stages {
        stage("Docker build") {
            agent any
            steps {
                echo " ============== Run container =================="
                sh 'docker run -p 8888:8080 --name demo reskeartur/k8sweb'
            }
        }
    }
}