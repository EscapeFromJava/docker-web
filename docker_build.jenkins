pipeline {
    agent any
    stages {
        stage("create docker image") {
            steps {
                echo " ============== start building image =================="
                echo ls
                dir ('') {
                	sh 'docker build . '
                }
            }
        }
    }
}